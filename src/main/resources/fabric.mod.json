{
  "schemaVersion": 1,
  "id": "minebot-mod",
  "version": "${version}",

  "name": "minebot control mod",
  "description": "Runs a local WebSocket control channel inside the real Minecraft client so an external Python process can drive movement/actions using real game physics instead of a protocol-level reimplementation.",
  "authors": [],
  "contact": {},

  "environment": "client",
  "entrypoints": {
    "client": [
      "minebot.mod.MinebotMod"
    ],
    "modmenu": [
      "minebot.mod.config.ModMenuIntegration"
    ]
  },

  "depends": {
    "fabricloader": ">=0.14.0",
    "fabric-api": "*",
    "minecraft": ">=26.1"
  },
  "suggests": {
    "modmenu": "*"
  },
  "mixins": [
    "minebot-mod.mixins.json"
  ]
}
